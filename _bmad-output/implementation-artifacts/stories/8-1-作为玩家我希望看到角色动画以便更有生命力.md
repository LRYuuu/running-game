# Story 8-1: 作为玩家，我希望看到角色动画以便更有生命力

**Epic:** Epic 8 - Polish 与优化
**优先级:** P1
**估时:** 2h
**状态:** ready-for-dev

---

## Story

作为玩家，
我希望看到角色动画，
以便更有生命力。

---

## Acceptance Criteria

### 功能验收标准

1. ** idle 动画**
   - Given 玩家静止站立
   - When 玩家未输入移动指令时
   - Then 播放 idle 呼吸/待机动画

2. **跑步动画**
   - Given 玩家在地面移动
   - When 玩家输入水平方向指令时
   - Then 播放跑步动画

3. **跳跃动画**
   - Given 玩家离开地面
   - When 玩家处于空中状态时
   - Then 播放跳跃动画

4. **落地动画**
   - Given 玩家从空中下落
   - When 玩家接触地面时
   - Then 播放落地缓冲动画（短暂）

5. **动画过渡平滑**
   - Given 动画状态切换
   - When 状态发生变化时
   - Then 动画过渡自然不生硬

### 技术验收标准

1. **使用 Unity Animator**
   - Given Unity 动画系统
   - When 控制角色动画时
   - Then 使用 Animator + AnimatorController

2. **动画参数控制**
   - Given 动画状态机
   - When 切换动画状态时
   - Then 使用参数驱动（isGrounded, speed, isJumping）

3. **代码规范**
   - Given 项目代码规范
   - When 修改现有脚本时
   - Then 遵循命名空间、字段命名、注释规范

---

## Dev Agent Guardrails

### 技术需求

1. **Unity 动画系统**
   - Animator 组件控制动画播放
   - AnimatorController 定义状态机
   - AnimationClip 定义具体动画

2. **现有脚本**
   - PlayerJumpController 位于 `Assets/Scripts/Player/PlayerJumpController.cs`
   - PlayerDeathController 位于 `Assets/Scripts/Player/PlayerDeathController.cs`
   - 需要创建 PlayerAnimationController.cs

3. **动画状态**
   - Idle（待机）
   - Run（跑步）
   - Jump（跳跃）
   - Fall（下落）
   - Land（落地）

### Architecture Compliance

**现有架构模式：**
- 命名空间：`RunnersJourney.Player`
- 单例模式：GameManager、AudioManager 使用单例
- 组件模式：玩家功能分散在多个控制器中

**文件位置规范：**
- 脚本：`Assets/Scripts/Player/`
- 动画控制器：`Assets/Animations/Player/`
- 动画剪辑：`Assets/Animations/Player/Clips/`

### Library/Framework Requirements

**Unity 动画 API：**
- `Animator.SetFloat(string hash, float value)` - 设置浮点参数
- `Animator.SetTrigger(string hash)` - 设置触发器
- `Animator.SetBool(string hash, bool value)` - 设置布尔参数

**Unity 版本：** 2022.3.62f3 LTS

### File Structure Requirements

**需要创建/修改的文件：**

| 文件 | 路径 | 操作 |
|------|------|------|
| `PlayerAnimator.controller` | `Assets/Animations/Player/PlayerAnimator.controller` | 新建 Animator Controller |
| `PlayerIdle.anim` | `Assets/Animations/Player/Clips/PlayerIdle.anim` | 新建 idle 动画 |
| `PlayerRun.anim` | `Assets/Animations/Player/Clips/PlayerRun.anim` | 新建跑步动画 |
| `PlayerJump.anim` | `Assets/Animations/Player/Clips/PlayerJump.anim` | 新建跳跃动画 |
| `PlayerFall.anim` | `Assets/Animations/Player/Clips/PlayerFall.anim` | 新建下落动画 |
| `PlayerLand.anim` | `Assets/Animations/Player/Clips/PlayerLand.anim` | 新建落地动画 |
| `PlayerAnimationController.cs` | `Assets/Scripts/Player/PlayerAnimationController.cs` | 新建动画控制器脚本 |
| `PlayerJumpController.cs` | `Assets/Scripts/Player/PlayerJumpController.cs` | 修改（集成动画触发） |
| `PlayerDeathController.cs` | `Assets/Scripts/Player/PlayerDeathController.cs` | 修改（集成死亡动画） |

---

## Previous Story Intelligence

### 来自 Story 7-2（跳跃音效）的 learnings

**已实现的功能：**
1. PlayerJumpController 已实现跳跃逻辑
2. 使用 `ExecuteJump()` 方法触发跳跃
3. 在跳跃时播放音效

**代码模式：**
```csharp
// 跳跃回调
private void ExecuteJump()
{
    // 播放音效
    if (_jumpSFX != null && AudioManager.Instance != null)
    {
        AudioManager.Instance.PlaySFX(_jumpSFX);
    }
    // ... 跳跃逻辑
}
```

### 现有玩家控制器结构

**PlayerJumpController 已包含：**
- 地面检测（GroundDetector）
- 一段跳/二段跳逻辑
- 跳跃缓冲
- 土狼时间

**PlayerDeathController 已包含：**
- 碰撞检测
- 死亡事件触发
- 重生逻辑

---

## Git Intelligence

**最近的提交模式：**
```
f98d50d docs(epic-7): 完成 Epic 7 音频系统回顾
89dca61 docs(story-7-3): 完成代码审查并更新状态
51b32ed feat(audio): 实现 UI 按钮点击音效播放 (Story 7-4)
```

**相关代码模式：**
- 玩家系统使用 `RunnersJourney.Player` 命名空间
- 公共方法必须添加 `/// <summary>` 文档注释
- 序列化字段必须添加 `[Header()]` 和 `[Tooltip()]`

---

## Implementation Guide

### 方案：创建 PlayerAnimationController

**创建 `PlayerAnimationController.cs`：**

```csharp
using UnityEngine;

namespace RunnersJourney.Player
{
    /// <summary>
    /// 玩家动画控制器 - 管理玩家角色的所有动画状态
    /// </summary>
    public class PlayerAnimationController : MonoBehaviour
    {
        #region 序列化字段
        [Header("动画组件")]
        [Tooltip("Animator 组件")]
        [SerializeField] private Animator _animator;

        [Header("动画参数 Hash（性能优化）")]
        [SerializeField] private string _speedParamHash = "Speed";
        [SerializeField] private string _isGroundedParamHash = "IsGrounded";
        [SerializeField] private string _jumpTriggerHash = "Jump";
        [SerializeField] private string _landTriggerHash = "Land";
        [SerializeField] private string _deathTriggerHash = "Death";
        #endregion

        #region 私有字段
        private int _speedHash;
        private int _isGroundedHash;
        private int _jumpHash;
        private int _landHash;
        private int _deathHash;
        #endregion

        #region Unity 生命周期
        private void Awake()
        {
            if (_animator == null)
            {
                _animator = GetComponent<Animator>();
            }

            // 预计算参数 Hash（性能优化）
            _speedHash = Animator.StringToHash(_speedParamHash);
            _isGroundedHash = Animator.StringToHash(_isGroundedParamHash);
            _jumpHash = Animator.StringToHash(_jumpTriggerHash);
            _landHash = Animator.StringToHash(_landTriggerHash);
            _deathHash = Animator.StringToHash(_deathTriggerHash);
        }
        #endregion

        #region 公共方法
        /// <summary>
        /// 更新移动速度（用于 Idle/Run 切换）
        /// </summary>
        /// <param name="speed">水平速度值</param>
        public void SetSpeed(float speed)
        {
            if (_animator != null)
            {
                _animator.SetFloat(_speedHash, Mathf.Abs(speed));
            }
        }

        /// <summary>
        /// 设置是否在地面
        /// </summary>
        /// <param name="isGrounded">是否在地面</param>
        public void SetGrounded(bool isGrounded)
        {
            if (_animator != null)
            {
                _animator.SetBool(_isGroundedHash, isGrounded);
            }
        }

        /// <summary>
        /// 触发跳跃动画
        /// </summary>
        public void TriggerJump()
        {
            if (_animator != null)
            {
                _animator.SetTrigger(_jumpHash);
            }
        }

        /// <summary>
        /// 触发落地动画
        /// </summary>
        public void TriggerLand()
        {
            if (_animator != null)
            {
                _animator.SetTrigger(_landHash);
            }
        }

        /// <summary>
        /// 触发死亡动画
        /// </summary>
        public void TriggerDeath()
        {
            if (_animator != null)
            {
                _animator.SetTrigger(_deathHash);
            }
        }
        #endregion
    }
}
```

### 集成到 PlayerJumpController

**修改 `PlayerJumpController.cs`：**

```csharp
// 添加引用
using RunnersJourney.Animation;

// 添加字段
[SerializeField] private PlayerAnimationController _animationController;

// 在 ExecuteJump() 中触发跳跃动画
if (_animationController != null)
{
    _animationController.TriggerJump();
}

// 在地面检测变化时调用
if (_animationController != null)
{
    _animationController.SetGrounded(isGrounded);
}
```

### Animator Controller 设置

1. **创建 PlayerAnimator.controller**
2. **添加状态：**
   - Any State → Jump（条件：Jump trigger）
   - Any State → Fall（条件：IsGrounded = false）
   - Idle ↔ Run（条件：Speed 参数）
   - Any State → Land（条件：Land trigger）
3. **添加参数：**
   - Speed（Float）
   - IsGrounded（Bool）
   - Jump（Trigger）
   - Land（Trigger）
   - Death（Trigger）

### 动画剪辑创建

可以使用以下方式创建动画：

1. **使用 Unity 动画窗口录制简单动画**
2. **从 Asset Store 下载 2D 角色动画包**
3. **使用 Aseprite 等工具制作精灵动画**

---

## Project Structure Notes

### 命名空间规范

| 路径 | 命名空间 |
|------|----------|
| Assets/Scripts/Player/ | RunnersJourney.Player |
| Assets/Animations/Player/ | （资源目录，无命名空间） |

### 代码规范

- 类/方法：`PascalCase`
- 私有字段：`_camelCase`（下划线前缀）
- 公共方法：必须添加 `/// <summary>` 文档注释
- 序列化字段：必须添加 `[Header()]` 和 `[Tooltip()]`
- 日志格式：`Debug.Log($"[ClassName] 消息")`

---

## Tasks

### Task 1: 创建动画资源 (AC: 1, 2, 3, 4)
- [ ] 创建 `Assets/Animations/Player/` 目录
- [ ] 创建 PlayerAnimator.controller
- [ ] 创建 PlayerIdle.anim（idle 呼吸动画）
- [ ] 创建 PlayerRun.anim（跑步动画）
- [ ] 创建 PlayerJump.anim（跳跃姿势）
- [ ] 创建 PlayerFall.anim（下落姿势）
- [ ] 创建 PlayerLand.anim（落地缓冲动画）

### Task 2: 创建 PlayerAnimationController.cs (AC: 5)
- [ ] 创建动画控制器脚本
- [ ] 实现速度更新方法
- [ ] 实现地面状态方法
- [ ] 实现跳跃/落地触发方法
- [ ] 实现死亡触发方法

### Task 3: 集成到现有控制器 (AC: 5)
- [ ] 修改 PlayerJumpController 添加动画调用
- [ ] 修改 PlayerDeathController 添加死亡动画

### Task 4: Unity 配置
- [ ] 在 Player GameObject 上添加 Animator 组件
- [ ] 分配 PlayerAnimator.controller
- [ ] 配置动画参数
- [ ] 测试动画过渡

### Task 5: 测试验证
- [ ] 测试 idle 动画正确播放
- [ ] 测试跑步动画正确播放
- [ ] 测试跳跃/落地动画正确播放
- [ ] 测试动画过渡平滑

---

## References

### 相关文件

| 文件 | 用途 |
|------|------|
| `PlayerJumpController.cs` | 跳跃逻辑，需要集成动画触发 |
| `PlayerDeathController.cs` | 死亡逻辑，需要集成死亡动画 |
| `GroundDetector.cs` | 地面检测，提供 isGrounded 状态 |

### 相关文档

1. **Unity 动画文档**
   - [Unity Manual: Animation](https://docs.unity3d.com/Manual/AnimationSection.html)
   - [Scripting API: Animator](https://docs.unity3d.com/ScriptReference/Animator.html)

2. **Epic 7 音频系统**
   - 音效播放模式可参考：`AudioManager.Instance.PlaySFX()`

---

## Dev Agent Record

### Agent Model Used

- **Model**: qwen3.5-plus
- **文档创建时间**: 2026-03-30

### Completion Notes

- ✅ 分析现有玩家控制器结构（PlayerJumpController、PlayerDeathController）
- ✅ 设计 PlayerAnimationController 架构
- ✅ 定义动画状态：Idle、Run、Jump、Fall、Land
- ✅ 定义动画参数：Speed（Float）、IsGrounded（Bool）、Jump/Land/Death（Trigger）
- ✅ 提供完整的实现代码示例

### Implementation Plan

**技术方法：**
- 创建独立的 PlayerAnimationController 组件
- 使用 Animator 参数驱动状态机
- 预计算参数 Hash 优化性能
- 与现有 PlayerJumpController、PlayerDeathController 集成

**代码变更：**
```csharp
// 新建 PlayerAnimationController.cs
// 提供 SetSpeed()、SetGrounded()、TriggerJump()、TriggerLand()、TriggerDeath() 方法

// 修改 PlayerJumpController.cs
// 在跳跃时调用 _animationController.TriggerJump()
// 在地面检测变化时调用 _animationController.SetGrounded()

// 修改 PlayerDeathController.cs
// 在死亡时调用 _animationController.TriggerDeath()
```

### File List

**新建文件:**
- `_bmad-output/implementation-artifacts/stories/8-1-作为玩家我希望看到角色动画以便更有生命力.md`
- `Assets/Animations/Player/PlayerAnimator.controller`（Unity 资源）
- `Assets/Animations/Player/Clips/*.anim`（动画剪辑）
- `Assets/Scripts/Player/PlayerAnimationController.cs`（动画控制器）

**修改文件:**
- `Assets/Scripts/Player/PlayerJumpController.cs`（集成动画调用）
- `Assets/Scripts/Player/PlayerDeathController.cs`（集成死亡动画）

---

**Story 8-1 文档创建完成 - 待实现**

**Epic 8 进度：**
- 8-1: 📄 文档已创建，待实现
- 8-2: backlog
- 8-3: backlog
- 8-4: backlog
- 8-5: backlog
- 8-6: backlog
