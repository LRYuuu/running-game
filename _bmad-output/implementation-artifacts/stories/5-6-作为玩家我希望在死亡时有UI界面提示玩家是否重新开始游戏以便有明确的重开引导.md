# Story 5-6: 作为玩家，我希望在死亡时有 UI 界面提示玩家是否重新开始游戏，以便有明确的重开引导

**Epic:** Epic 5 - UI 系统
**优先级:** P1
**估算:** 2h
**状态:** ready-for-dev

---

## Story

As a **玩家**,
I want **死亡时有 UI 界面提示重新开始游戏**,
so that **有明确的重开引导**。

---

## Acceptance Criteria

### 功能性验收标准

1. **死亡界面显示**
   - Given 玩家死亡触发 GameState.Dying
   - When 死亡流程开始
   - Then 显示死亡界面（Game Over UI）

2. **重新开始按钮**
   - Given 死亡界面显示
   - When 点击"重新开始"按钮
   - Then 调用 GameManager.RestartGame() 回到主界面

3. **死亡界面内容**
   - Given 死亡界面
   - When 显示时
   - Then 显示"游戏结束"文本和当前分数

4. **破纪录标志**
   - Given 玩家死亡且打破纪录
   - When 死亡界面显示
   - Then 显示"新纪录！"标志（金色）

5. **自动隐藏**
   - Given 死亡界面显示
   - When 玩家重新开始或回到主菜单
   - Then 死亡界面自动隐藏

### 技术性验收标准

1. **UI Toolkit 实现**
   - Given 死亡界面 UI
   - When 渲染
   - Then 使用 UI Toolkit (UXML/USS) 实现

2. **事件驱动**
   - Given GameManager 状态变化
   - When GameState.Dying 触发
   - Then UIManager 订阅并显示死亡界面

3. **UIManager 集成**
   - Given UIManager 管理 UI 状态
   - When 死亡时
   - Then 切换到 GameOver UI 状态

4. **代码规范**
   - Given 新增脚本
   - When 提交代码
   - Then 符合项目代码规范（命名空间、注释、日志）

---

## Tasks / Subtasks

### Task 1: 创建 GameOverUI 数据结构 (AC: 功能 1, 3, 4)
- [ ] 创建 GameOverUI.uxml（Resources/UI/ 文件夹）
- [ ] 添加容器：game-over-container
- [ ] 添加元素：标题、当前分数、最高分、新纪录标志、重新开始按钮、返回主菜单按钮

### Task 2: 创建 GameOverUI 样式 (AC: 功能 3, 4)
- [ ] 创建 GameOverUI.uss
- [ ] 居中布局样式
- [ ] 金色新纪录标志样式
- [ ] 按钮样式（开始游戏、返回主菜单）

### Task 3: 创建 GameOverUI.cs 控制器 (AC: 功能 1, 2, 4, 5, 技术 1, 2)
- [ ] 实现 Show() 方法显示界面
- [ ] 实现 Hide() 方法隐藏界面
- [ ] 实现 UpdateScoreDisplay() 显示当前分数
- [ ] 实现 ShowNewRecordBadge() 显示新纪录标志
- [ ] 订阅 GameManager 事件自动显示/隐藏

### Task 4: 集成到 UIManager (AC: 技术 3)
- [ ] 添加 GameOverUI 引用
- [ ] 在 OnGameStateChanged 中处理 GameState.Dying
- [ ] 添加新的 UIState.GameOver（可选）

### Task 5: 编写测试用例
- [ ] EditMode 测试：GameOverUI 显示/隐藏逻辑
- [ ] PlayMode 测试：死亡界面实际显示验证

---

## Architecture & Design

### UI 层次结构

```
┌─────────────────────────────────────────────────────────┐
│  GameOverUIPanel (UIDocument root)                       │
├─────────────────────────────────────────────────────────┤
│  ├── GameOverContainer (VisualElement)                   │
│  │     name: "game-over-container"                       │
│  │     class: .game-over-container                       │
│  │                                                       │
│  │   ├── TitleLabel (Label)                              │
│  │   │     name: "game-over-title"                       │
│  │   │     text: "游戏结束"                              │
│  │   │     class: .game-over-title                       │
│  │   │                                                   │
│  │   ├── ScoreLabel (Label)                              │
│  │   │     name: "final-score-label"                     │
│  │   │     text: "分数：0"                               │
│  │   │     class: .game-over-score                       │
│  │   │                                                   │
│  │   ├── NewRecordBadge (VisualElement)                  │
│  │   │     name: "new-record-badge"                      │
│  │   │     class: .new-record-badge                      │
│  │   │     │                                             │
│  │   │     └── NewRecordLabel (Label)                    │
│  │   │           name: "new-record-badge-label"          │
│  │   │           text: "新纪录！"                        │
│  │   │                                                   │
│  │   ├── RestartButton (Button)                          │
│  │   │     name: "restart-button"                        │
│  │   │     text: "重新开始"                              │
│  │   │     class: .restart-button                        │
│  │   │                                                   │
│  │   └── MainMenuButton (Button)                         │
│  │         name: "main-menu-button"                      │
│  │         text: "返回主菜单"                            │
│  │         class: .main-menu-button                      │
└─────────────────────────────────────────────────────────┘
```

### 状态流转

```
┌──────────────┐      死亡       ┌──────────────┐
│   InGame     │ ──────────────▶ │   GameOver   │
│   (游戏中)   │                 │   (死亡界面)  │
└──────────────┘                 └──────┬───────┘
       ▲                                │
       │ 重新开始                        │ 点击重新开始
       │                                ▼
       │                         ┌──────────────┐
       │                         │   Playing    │
       │                         │   (游戏中)   │
       │                         └──────────────┘
       │                                │
       │ 返回主菜单                      │ 点击返回主菜单
       └────────────────────────────────┘
                                        ▼
                                 ┌──────────────┐
                                 │  MainMenu    │
                                 │  (主界面)    │
                                 └──────────────┘
```

### 文件位置

| 文件 | 路径 | 操作 |
|------|------|------|
| `GameOverUI.uxml` | `Assets/Resources/UI/GameOverUI.uxml` | 新增 |
| `GameOverUI.uss` | `Assets/Resources/UI/GameOverUI.uss` | 新增 |
| `GameOverUI.cs` | `Assets/Scripts/UI/GameOverUI.cs` | 新增 |
| `UIManager.cs` | `Assets/Scripts/UI/UIManager.cs` | 修改 |
| `UIState.cs` | `Assets/Scripts/UI/UIState.cs` | 修改（添加 GameOver 状态） |

---

## Implementation Notes

### GameOverUI.uxml

```xml
<ui:UIDocument xmlns:ui="http://unity3d.com/ui">
    <VisualElement name="game-over-container" class="game-over-container">
        <Label name="game-over-title" text="游戏结束" class="game-over-title" />
        <Label name="final-score-label" text="分数：0" class="game-over-score" />
        <Label name="highscore-display-label" text="最高分：0" class="game-over-highscore" />

        <!-- 新纪录标志（默认隐藏） -->
        <VisualElement name="new-record-badge" class="new-record-badge">
            <Label name="new-record-badge-label" text="新纪录！" class="new-record-badge-label" />
        </VisualElement>

        <Button name="restart-button" text="重新开始" class="restart-button" />
        <Button name="main-menu-button" text="返回主菜单" class="main-menu-button" />
    </VisualElement>
</ui:UIDocument>
```

### GameOverUI.uss

```css
.game-over-container {
    position: absolute;
    left: 50%;
    top: 50%;
    translate: -50% -50%;
    align-items: center;
    justify-content: center;
    gap: 20px;
}

.game-over-title {
    font-size: 64px;
    color: rgb(255, 100, 100);
    -unity-font-style: bold;
    -unity-shadow-color: rgba(0, 0, 0, 0.8);
    -unity-shadow-offset: 4px 4px;
}

.game-over-score {
    font-size: 32px;
    color: rgb(255, 255, 255);
    -unity-font-style: bold;
}

.game-over-highscore {
    font-size: 24px;
    color: rgb(255, 215, 0);
    -unity-font-style: bold;
}

.new-record-badge {
    margin-top: 10px;
}

.new-record-badge-label {
    font-size: 48px;
    color: rgb(255, 215, 0);
    -unity-font-style: bold;
    -unity-shadow-color: rgba(0, 0, 0, 0.8);
    -unity-shadow-offset: 4px 4px;
}

.restart-button, .main-menu-button {
    min-width: 200px;
    min-height: 60px;
    font-size: 24px;
    -unity-font-style: bold;
}

.restart-button {
    background-color: rgb(100, 200, 100);
    color: rgb(255, 255, 255);
}

.main-menu-button {
    background-color: rgb(100, 100, 200);
    color: rgb(255, 255, 255);
}
```

### GameOverUI.cs

```csharp
using UnityEngine;
using UnityEngine.UIElements;
using SquareFireline.Game;

namespace SquareFireline.UI
{
    [RequireComponent(typeof(UIDocument))]
    public class GameOverUI : MonoBehaviour
    {
        #region 序列化字段
        [Header("UI 文档引用")]
        [Tooltip("游戏结束 UI UXML 文件")]
        [SerializeField] private UIDocument uiDocument;
        #endregion

        #region 私有字段
        private VisualElement _root;
        private Label _scoreLabel;
        private Label _highScoreLabel;
        private VisualElement _newRecordBadge;
        private UnityEngine.UIElements.Button _restartButton;
        private UnityEngine.UIElements.Button _mainMenuButton;
        private bool _isSubscribed = false;
        #endregion

        #region Unity 生命周期
        private void Awake()
        {
            if (uiDocument == null)
            {
                uiDocument = GetComponent<UIDocument>();
            }

            if (uiDocument != null)
            {
                var root = uiDocument.rootVisualElement;
                if (root != null)
                {
                    root.style.display = DisplayStyle.None;
                }
            }
        }

        private void OnEnable()
        {
            StartCoroutine(InitializeUIAfterDelay());
        }

        private void OnDisable()
        {
            if (_isSubscribed && GameManager.Instance != null)
            {
                GameManager.Instance.OnGameStateChanged -= OnGameStateChanged;
            }

            if (_isSubscribed && ScoreManager.Instance != null)
            {
                ScoreManager.Instance.OnScoreChanged -= OnScoreChanged;
            }
        }
        #endregion

        #region 公共方法
        public void Show()
        {
            if (_root != null)
            {
                _root.style.display = DisplayStyle.Flex;
            }
        }

        public void Hide()
        {
            if (_root != null)
            {
                _root.style.display = DisplayStyle.None;
            }
        }

        public void UpdateScoreDisplay(int currentScore, int highScore)
        {
            if (_scoreLabel != null)
            {
                _scoreLabel.text = $"分数：{currentScore}";
            }
            if (_highScoreLabel != null)
            {
                _highScoreLabel.text = $"最高分：{highScore}";
            }
        }

        public void ShowNewRecordBadge()
        {
            if (_newRecordBadge != null)
            {
                _newRecordBadge.style.display = DisplayStyle.Flex;
            }
        }

        public void HideNewRecordBadge()
        {
            if (_newRecordBadge != null)
            {
                _newRecordBadge.style.display = DisplayStyle.None;
            }
        }
        #endregion

        #region 私有方法
        private System.Collections.IEnumerator InitializeUIAfterDelay()
        {
            yield return null;

            _root = uiDocument.rootVisualElement;
            if (_root != null)
            {
                InitializeUIElements();

                // 订阅事件
                if (GameManager.Instance != null)
                {
                    GameManager.Instance.OnGameStateChanged += OnGameStateChanged;
                }
                if (ScoreManager.Instance != null)
                {
                    ScoreManager.Instance.OnScoreChanged += OnScoreChanged;
                }
                _isSubscribed = true;
            }
        }

        private void InitializeUIElements()
        {
            _scoreLabel = _root.Q<Label>("final-score-label");
            _highScoreLabel = _root.Q<Label>("highscore-display-label");
            _newRecordBadge = _root.Q<VisualElement>("new-record-badge");
            _restartButton = _root.Q<UnityEngine.UIElements.Button>("restart-button");
            _mainMenuButton = _root.Q<UnityEngine.UIElements.Button>("main-menu-button");

            // 默认隐藏新纪录标志
            if (_newRecordBadge != null)
            {
                _newRecordBadge.style.display = DisplayStyle.None;
            }

            // 注册按钮事件
            if (_restartButton != null)
            {
                _restartButton.clicked += OnRestartButtonClicked;
            }
            if (_mainMenuButton != null)
            {
                _mainMenuButton.clicked += OnMainMenuButtonClicked;
            }
        }

        private void OnGameStateChanged(GameState oldState, GameState newState)
        {
            switch (newState)
            {
                case GameState.Dying:
                    Show();
                    break;
                case GameState.Playing:
                case GameState.Waiting:
                    Hide();
                    break;
            }
        }

        private void OnScoreChanged(int currentScore, int highScore)
        {
            UpdateScoreDisplay(currentScore, highScore);
        }

        private void OnRestartButtonClicked()
        {
            if (GameManager.Instance != null)
            {
                GameManager.Instance.RestartGame();
            }
        }

        private void OnMainMenuButtonClicked()
        {
            if (GameManager.Instance != null)
            {
                GameManager.Instance.RestartGame();
            }
        }

        private void OnNewRecord(int score)
        {
            ShowNewRecordBadge();
        }
        #endregion
    }
}
```

---

## Testing Requirements

### EditMode 测试

```csharp
using NUnit.Framework;
using UnityEngine;
using UnityEngine.UIElements;
using SquareFireline.UI;

namespace SquareFireline.Tests.EditMode.UI
{
    [TestFixture]
    public class GameOverUITests
    {
        private GameOverUI _gameOverUI;
        private GameObject _testObject;

        [SetUp]
        public void Setup()
        {
            _testObject = new GameObject("GameOverUITest");
            _testObject.AddComponent<UIDocument>();
            _gameOverUI = _testObject.AddComponent<GameOverUI>();
        }

        [TearDown]
        public void TearDown()
        {
            if (_testObject != null)
            {
                Object.DestroyImmediate(_testObject);
            }
        }

        [Test]
        public void Show_EnablesRootVisualElement()
        {
            // Arrange & Act
            _gameOverUI.Show();

            // Assert
            // 验证 Show 方法调用成功（需要 mock UIDocument）
            Assert.Pass("Show() called successfully");
        }

        [Test]
        public void Hide_DisablesRootVisualElement()
        {
            // Arrange & Act
            _gameOverUI.Hide();

            // Assert
            Assert.Pass("Hide() called successfully");
        }
    }
}
```

---

## Acceptance Status

| AC | Status | Notes |
|----|--------|-------|
| AC1: 死亡界面显示 | ⏳ Backlog | 待实现 |
| AC2: 重新开始按钮 | ⏳ Backlog | 待实现 |
| AC3: 死亡界面内容 | ⏳ Backlog | 待实现 |
| AC4: 破纪录标志 | ⏳ Backlog | 待实现 |
| AC5: 自动隐藏 | ⏳ Backlog | 待实现 |
| AC 技术 1: UI Toolkit | ⏳ Backlog | 待实现 |
| AC 技术 2: 事件驱动 | ⏳ Backlog | 待实现 |
| AC 技术 3: UIManager 集成 | ⏳ Backlog | 待实现 |
| AC 技术 4: 代码规范 | ⏳ Backlog | 待实现 |

---

## Next Actions

1. 创建 GameOverUI.uxml 和 GameOverUI.uss 文件
2. 创建 GameOverUI.cs 控制器
3. 集成到 UIManager
4. 运行测试验证功能
5. 更新 sprint-status.yaml

**Story 状态:** ready-for-dev

---

*文档创建时间*: 2026-03-25
*文档版本*: 1.0
*状态:* Ready for Dev
