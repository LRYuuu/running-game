# Story 7-4: 作为系统，我可以播放 UI 音效以便点击有反馈

**Epic:** Epic 7 - 音频系统
**优先级:** P1
**估时:** 1h
**状态:** ready-for-dev

---

## Story

作为系统，
我可以播放 UI 音效，
以便点击有反馈。

---

## Acceptance Criteria

### 功能验收标准

1. **按钮点击时播放音效**
   - Given 主界面或设置面板显示中
   - When 玩家点击任意按钮时
   - Then 播放按钮点击音效

2. **音效音量控制**
   - Given 按钮点击音效正在播放
   - When 调整 SFX 音量设置时
   - Then 按钮点击音效音量随之改变

3. **快速响应**
   - Given 玩家点击按钮
   - Then 音效响应延迟 <100ms

4. **音效类型区分**
   - Given 不同类型的 UI 操作（开始游戏、设置、关闭等）
   - Then 可播放不同的音效（可选）

### 技术验收标准

1. **使用现有 AudioManager**
   - Given AudioManager 单例已存在
   - When 播放按钮点击音效时
   - Then 调用 AudioManager.PlaySFX() 方法

2. **音效资源管理**
   - Given 需要播放按钮点击音效
   - When 调用 PlaySFX 时
   - Then 使用 AudioClip 资源

3. **UI Toolkit 集成**
   - Given UI Toolkit 按钮系统
   - When 按钮被点击时
   - Then 在回调中触发音效播放

4. **代码规范**
   - Given 项目代码规范
   - When 修改现有脚本时
   - Then 遵循命名空间、字段命名、注释规范

---

## Dev Agent Guardrails

### 技术需求

1. **音频系统架构**
   - AudioManager 已实现单例模式，位于 `Assets/Scripts/Audio/AudioManager.cs`
   - PlaySFX(AudioClip clip) 方法已实现，使用 PlayOneShot 模式
   - PlaySFX(AudioClip clip, float volume) 支持带音量播放
   - AudioConfig 位于 `Assets/Resources/Audio/AudioConfig.asset`
   - SFX 目录位于 `Assets/Audio/SFX/`

2. **UI 系统架构**
   - MainMenuUI 位于 `Assets/Scripts/UI/MainMenuUI.cs` - 主界面控制器
   - SettingsPanel 位于 `Assets/Scripts/UI/SettingsPanel.cs` - 设置面板控制器
   - UI Toolkit 使用 VisualElement 和 Button 组件
   - 按钮事件使用 `button.clicked += Callback` 注册

3. **性能要求**
   - 音效响应延迟 <100ms
   - 使用 PlayOneShot 确保并发播放不中断

### Architecture Compliance

**现有架构模式：**
- 命名空间：`RunnersJourney.Audio`（音频系统）、`RunnersJourney.UI`（UI 系统）
- 单例模式：AudioManager 使用单例模式 + DontDestroyOnLoad
- 事件驱动：按钮点击事件使用 C# 事件委托

**文件位置规范：**
- 脚本：`Assets/Scripts/{模块}/`
- 资源配置：`Assets/Resources/{模块}/`
- 音频资源：`Assets/Audio/SFX/`

### Library/Framework Requirements

**Unity 音频 API：**
- `AudioSource.PlayOneShot(AudioClip clip)` - 播放音效（支持并发）
- `AudioSource.PlayOneShot(AudioClip clip, float volume)` - 带音量控制播放
- `AudioManager.PlaySFX()` - 已封装的音效播放方法

**Unity UI Toolkit API：**
- `Button.clicked` - 按钮点击事件
- `VisualElement.Q<T>()` - 查询 UI 元素

**Unity 版本：** 2022.3.62f3 LTS

### File Structure Notes

**需要创建/修改的文件：**

| 文件 | 路径 | 操作 |
|------|------|------|
| `button-click.mp3` 或 `button-click.wav` | `Assets/Audio/SFX/button-click.mp3` | 新建音频资源 |
| `MainMenuUI.cs` | `Assets/Scripts/UI/MainMenuUI.cs` | 修改（添加音效播放） |
| `SettingsPanel.cs` | `Assets/Scripts/UI/SettingsPanel.cs` | 修改（添加音效播放） |

---

## Previous Story Intelligence

### 来自 Story 7-2（跳跃音效播放）的 learnings

**已实现的功能：**
1. 跳跃音效在 PlayerJumpController.ExecuteJump() 中播放
2. 使用 `AudioManager.Instance.PlaySFX(_jumpSFX)` 调用
3. 音效文件位于 `Assets/Audio/SFX/jump.mp3`

**代码模式：**
```csharp
// PlayerJumpController.cs 中的实现
if (_jumpSFX != null && AudioManager.Instance != null)
{
    AudioManager.Instance.PlaySFX(_jumpSFX);
}
```

### 来自 Story 7-3（碰撞音效播放）的 learnings

**已实现的功能：**
1. 碰撞音效在 PlayerDeathController.Die() 中播放
2. 使用 `AudioManager.Instance.PlaySFX(_collisionSFX, 0.4f)` 调用
3. 音效文件位于 `Assets/Audio/SFX/collision.mp3`
4. 音量通过代码参数控制（0.4f = 40%）

**代码模式：**
```csharp
// PlayerDeathController.cs 中的实现
if (_collisionSFX != null && AudioManager.Instance != null)
{
    AudioManager.Instance.PlaySFX(_collisionSFX, 0.4f);
}
```

### 来自 Story 7-5（音量控制）的 learnings

**已实现的功能：**
1. SettingsPanel 支持 BGM 和 SFX 音量调节
2. PlayerPrefs 持久化音量设置
3. AudioManager 在 Start() 中加载保存的音量

**代码模式：**
```csharp
// 音量变化回调
private void OnSFXVolumeChanged(ChangeEvent<float> evt)
{
    float newVolume = Mathf.Clamp01(evt.newValue);
    if (AudioManager.Instance != null)
    {
        AudioManager.Instance.SetSFXVolume(newVolume);
    }
    PlayerPrefs.SetFloat("SFXVolume", newVolume);
    PlayerPrefs.Save();
}
```

---

## Git Intelligence

**最近的提交模式：**
```
95cb73a feat(ui): 实现音量控制设置面板 (Story 7-5)
946a27d feat(audio): 实现跳跃音效播放功能 (Story 7-2)
2aa93d3 feat(audio): 实现背景音乐播放系统 (Story 7-1)
```

**相关代码模式：**
- UI 系统使用 `RunnersJourney.UI` 命名空间
- 音频系统使用 `RunnersJourney.Audio` 命名空间
- 公共方法必须添加 `/// <summary>` 文档注释
- 序列化字段必须添加 `[Header()]` 和 `[Tooltip()]`
- 日志格式：`Debug.Log($"[ClassName] 消息")`

---

## Implementation Guide

### 方案：在各 UI 控制器中添加音效播放

**修改 `MainMenuUI.cs`：**

```csharp
using UnityEngine;
using UnityEngine.UIElements;
using RunnersJourney.Audio;

namespace RunnersJourney.UI
{
    public class MainMenuUI : MonoBehaviour
    {
        #region 序列化字段
        [Header("音频")]
        [Tooltip("按钮点击音效剪辑")]
        [SerializeField] private AudioClip _buttonClickSFX;
        #endregion

        #region 私有字段
        private Button _startButton;
        private Button _biomeSelectionButton;
        private Button _settingsButton;
        #endregion

        #region 私有方法
        /// <summary>
        /// 播放按钮点击音效
        /// </summary>
        private void PlayButtonClickSFX()
        {
            if (_buttonClickSFX != null && AudioManager.Instance != null)
            {
                AudioManager.Instance.PlaySFX(_buttonClickSFX);
            }
        }

        /// <summary>
        /// 开始游戏按钮点击回调
        /// </summary>
        private void OnStartButtonClicked()
        {
            PlayButtonClickSFX();
            StartGame();
        }

        /// <summary>
        /// 选择群系按钮点击回调
        /// </summary>
        private void OnBiomeSelectionButtonClicked()
        {
            PlayButtonClickSFX();
            // 打开群系选择面板
        }

        /// <summary>
        /// 设置按钮点击回调
        /// </summary>
        private void OnSettingsButtonClicked()
        {
            PlayButtonClickSFX();
            // 打开设置面板
        }
        #endregion
    }
}
```

**修改 `SettingsPanel.cs`：**

```csharp
using UnityEngine;
using UnityEngine.UIElements;
using RunnersJourney.Audio;

namespace RunnersJourney.UI
{
    public class SettingsPanel : MonoBehaviour
    {
        #region 序列化字段
        [Header("音频")]
        [Tooltip("按钮点击音效剪辑")]
        [SerializeField] private AudioClip _buttonClickSFX;
        #endregion

        #region 私有字段
        private Button _closeButton;
        #endregion

        #region 私有方法
        /// <summary>
        /// 播放按钮点击音效
        /// </summary>
        private void PlayButtonClickSFX()
        {
            if (_buttonClickSFX != null && AudioManager.Instance != null)
            {
                AudioManager.Instance.PlaySFX(_buttonClickSFX);
            }
        }

        /// <summary>
        /// 关闭按钮点击回调
        /// </summary>
        private void OnCloseClicked()
        {
            PlayButtonClickSFX();
            Hide();
        }
        #endregion
    }
}
```

### 音频资源准备

1. **创建音效文件**
   - 路径：`Assets/Audio/SFX/button-click.mp3` 或 `button-click.wav`
   - 风格：简短、清脆的点击声
   - 时长：0.05-0.15 秒

2. **Unity 导入设置**
   - Force To Mono: ✓（单声道）
   - Load Type: Decompress On Load（低延迟）
   - Preload Audio Data: ✓

### 扩展方案：统一 UI 音效管理器（可选）

如果项目中 UI 按钮数量较多，可以考虑创建统一的 UI 音效管理器：

```csharp
using UnityEngine;
using UnityEngine.UIElements;

namespace RunnersJourney.UI
{
    /// <summary>
    /// UI 音效管理器 - 统一管理所有 UI 音效
    /// </summary>
    public class UISoundManager : MonoBehaviour
    {
        #region 单例
        private static UISoundManager _instance;

        public static UISoundManager Instance
        {
            get
            {
                if (_instance == null)
                {
                    _instance = FindObjectOfType<UISoundManager>();
                    if (_instance == null)
                    {
                        var go = new GameObject("UISoundManager");
                        _instance = go.AddComponent<UISoundManager>();
                    }
                }
                return _instance;
            }
        }
        #endregion

        #region 序列化字段
        [Header("音效")]
        [Tooltip("按钮点击音效")]
        [SerializeField] private AudioClip _buttonClickSFX;
        #endregion

        #region 公共方法
        /// <summary>
        /// 播放按钮点击音效
        /// </summary>
        public void PlayButtonClickSFX()
        {
            if (_buttonClickSFX != null && AudioManager.Instance != null)
            {
                AudioManager.Instance.PlaySFX(_buttonClickSFX);
            }
        }
        #endregion
    }
}
```

使用方式：
```csharp
// 在按钮回调中调用
private void OnButtonClicked()
{
    UISoundManager.Instance?.PlayButtonClickSFX();
    // 其他逻辑
}
```

---

## Project Structure Notes

### 音频资源目录规范

| 目录 | 用途 | 示例 |
|------|------|------|
| `Assets/Audio/BGM/` | 背景音乐 (Background Music) | background.mp3 |
| `Assets/Audio/SFX/` | 音效 (Sound Effects) | jump.mp3, collision.mp3, button-click.mp3 |

### 命名空间规范

| 路径 | 命名空间 |
|------|----------|
| Assets/Scripts/UI/ | RunnersJourney.UI |
| Assets/Scripts/Audio/ | RunnersJourney.Audio |

### 代码规范

- 类/方法：`PascalCase`
- 私有字段：`_camelCase`（下划线前缀）
- 公共方法：必须添加 `/// <summary>` 文档注释
- 序列化字段：必须添加 `[Header()]` 和 `[Tooltip()]`
- 日志格式：`Debug.Log($"[ClassName] 消息")`

---

## Tasks

### Task 1: 准备音频资源 (AC: 1, 3)
- [ ] 准备或创建按钮点击音效文件（button-click.mp3 或 button-click.wav）
- [ ] 将音效文件放入 `Assets/Audio/SFX/` 目录
- [ ] 在 Unity 中配置导入设置（Decompress On Load）

### Task 2: 修改 MainMenuUI (AC: 1, 2, 3)
- [ ] 添加 `AudioManager` 命名空间引用
- [ ] 添加 `_buttonClickSFX` 序列化字段（AudioClip 类型）
- [ ] 添加 `PlayButtonClickSFX()` 私有方法
- [ ] 在所有按钮点击回调中调用 `PlayButtonClickSFX()`
- [ ] 添加空值检查和日志输出

### Task 3: 修改 SettingsPanel (AC: 1, 2, 3)
- [ ] 添加 `AudioManager` 命名空间引用
- [ ] 添加 `_buttonClickSFX` 序列化字段（AudioClip 类型）
- [ ] 添加 `PlayButtonClickSFX()` 私有方法
- [ ] 在关闭按钮点击回调中调用 `PlayButtonClickSFX()`
- [ ] 添加空值检查和日志输出

### Task 4: Unity Inspector 配置
- [ ] 在 Unity 中打开 MainMenuUI GameObject
- [ ] 将按钮点击音效文件拖入 MainMenuUI 的 Button Click SFX 字段
- [ ] 在 Unity 中打开 SettingsPanel GameObject
- [ ] 将按钮点击音效文件拖入 SettingsPanel 的 Button Click SFX 字段

### Task 5: 测试验证
- [ ] 测试主界面所有按钮点击时音效正确播放
- [ ] 测试设置面板按钮点击时音效正确播放
- [ ] 测试响应延迟 <100ms
- [ ] 测试 SFX 音量设置对按钮点击音效的影响

---

## References

### 相关文件

| 文件 | 用途 |
|------|------|
| `AudioManager.cs` | 音频管理器单例，提供 PlaySFX() 方法 |
| `MainMenuUI.cs` | 主界面控制器，包含开始游戏、群系选择、设置按钮 |
| `SettingsPanel.cs` | 设置面板控制器，包含关闭按钮 |
| `AudioConfig.asset` | 音频配置，包含 sfxVolume 设置 |
| `PlayerJumpController.cs` | 跳跃音效实现参考 |
| `PlayerDeathController.cs` | 碰撞音效实现参考 |

### 相关文档

1. **Story 7-1 实现文档**
   - [Source: `_bmad-output/implementation-artifacts/stories/7-1-作为系统我可以播放背景音乐以便营造氛围.md`]

2. **Story 7-2 实现文档**
   - [Source: `_bmad-output/implementation-artifacts/stories/7-2-作为系统我可以播放跳跃音效以便提供反馈.md`]

3. **Story 7-3 实现文档**
   - [Source: `_bmad-output/implementation-artifacts/stories/7-3-作为系统我可以播放碰撞音效以便明确死亡.md`]

4. **Story 7-5 实现文档**
   - [Source: `_bmad-output/implementation-artifacts/stories/7-5-作为玩家我可以调整音量以便适配偏好.md`]

5. **GDD 音频需求**
   - UI 音效：简短、清脆
   - 响应时间：<100ms
   - [Source: `_bmad-output/gdd.md#L546`]

---

## Dev Agent Record

### Agent Model Used

- **Model**: qwen3.5-plus
- **文档创建时间**: 2026-03-30

### Completion Notes

- ✅ 分析现有 AudioManager 实现，确认 PlaySFX() 方法可用
- ✅ 分析 MainMenuUI 和 SettingsPanel，确定按钮点击回调位置
- ✅ 参考 Story 7-2 和 Story 7-3 的音效实现模式（在事件源处播放）
- ✅ GDD 明确要求 UI 音效"简短、清脆"
- ✅ 提供两种实现方案：
  1. 分散式（推荐）：在各 UI 控制器中添加音效播放
  2. 集中式（可选）：创建 UISoundManager 统一管理
- ✅ 架构统一：所有玩家相关音效在玩家控制器，UI 相关音效在 UI 控制器

### Implementation Plan

**技术方法：**
- 在各 UI 控制器（MainMenuUI、SettingsPanel）中添加 `_buttonClickSFX` 字段
- 创建统一的 `PlayButtonClickSFX()` 私有方法
- 在所有按钮点击回调中调用该方法
- 使用 AudioManager.Instance.PlaySFX() 确保音量控制生效

**代码变更：**
```csharp
// 添加命名空间
using RunnersJourney.Audio;

// 添加序列化字段
[Header("音频")]
[Tooltip("按钮点击音效剪辑")]
[SerializeField] private AudioClip _buttonClickSFX;

// 添加音效播放方法
private void PlayButtonClickSFX()
{
    if (_buttonClickSFX != null && AudioManager.Instance != null)
    {
        AudioManager.Instance.PlaySFX(_buttonClickSFX);
    }
}

// 在按钮回调中调用
private void OnButtonClicked()
{
    PlayButtonClickSFX();
    // 其他逻辑
}
```

### File List

**新建文件:**
- `_bmad-output/implementation-artifacts/stories/7-4-作为系统我可以播放 UI 音效以便点击有反馈.md`

**待创建:**
- `Assets/Audio/SFX/button-click.mp3`（需准备音频资源）

**修改文件:**
- `Assets/Scripts/UI/MainMenuUI.cs`（添加按钮点击音效播放）
- `Assets/Scripts/UI/SettingsPanel.cs`（添加按钮点击音效播放）

### Change Log

文档创建完成，待实现以下内容：
1. 准备 button-click.mp3 音效资源
2. 修改 MainMenuUI.cs 添加音效播放
3. 修改 SettingsPanel.cs 添加音效播放
4. 在 Unity Inspector 中分配音效引用
5. 测试验证

---

**Story 7-4 文档创建完成 - 待实现**

**Epic 7 进度:**
- 7-1: ✅ 背景音乐播放
- 7-2: ✅ 跳跃音效播放
- 7-3: ✅ 碰撞音效播放
- 7-4: 📄 文档已创建，待实现
- 7-5: ✅ 音量控制

**Epic 7 完成度：4/5 完成（80%）**
