# Story 3-1: 作为系统，我可以在地图中生成障碍物，以便增加挑战性

**Epic:** Epic 3 - 障碍物与碰撞
**优先级:** P0
**估算:** 2h
**状态:** Ready for Review

---

## 任务列表

- [x] 1. 障碍物生成逻辑实现
- [x] 2. 障碍物 Tile 池配置
- [x] 3. 障碍物生成位置控制（生成在草坪层上方）
- [x] 4. 障碍物与空隙生成互斥（空隙上方不生成障碍物）
- [x] 5. 单元测试验证

---

## 实现摘要

### 1. 障碍物生成逻辑 (`TilemapEndlessMapGenerator.cs`)

**位置:** `Assets/Scripts/Map/TilemapEndlessMapGenerator.cs`

**核心方法:**

```csharp
// 判断是否应该生成障碍物
private bool ShouldSpawnObstacle(int worldX)
{
    // 检查当前列高度与前一列的高度差
    if (worldX > 0)
    {
        int currentHeight = GetColumnHeight(worldX);
        int prevHeight = GetColumnHeight(worldX - 1);

        // 高度差 > 0 时不生成障碍物（避免障碍物在空中或地下）
        if (Mathf.Abs(currentHeight - prevHeight) > 0)
        {
            return false;
        }
    }

    // 检查与上一个障碍物的间隔
    if (worldX - lastObstacleWorldX < currentObstacleGap)
    {
        return false;
    }

    // 概率判定
    if (Random.value > config.obstacleSpawnChance)
    {
        return false;
    }

    // 更新间隔
    currentObstacleGap = Random.Range(config.minObstacleGap, config.maxObstacleGap + 1);
    return true;
}

// 生成障碍物
private void SpawnObstacle(int worldX)
{
    if (obstacleTilemap == null) { ... }
    if (config.obstacleTiles == null || config.obstacleTiles.Length == 0) { ... }

    // 获取当前列高度，障碍物生成在草坪层上方
    int columnHeight = GetColumnHeight(worldX);
    Vector3Int obsPos = new Vector3Int(worldX, columnHeight, 0);

    // 从障碍物池中随机选择一个 Tile
    TileBase obstacleTile = config.obstacleTiles[Random.Range(0, config.obstacleTiles.Length)];

    obstacleTilemap.SetTile(obsPos, obstacleTile);
    lastObstacleWorldX = worldX;
}
```

**生成规则:**
1. 只在高度差为 0 的列生成（避免在空中或地下）
2. 与上一个障碍物保持最小间隔
3. 按配置概率生成
4. 空隙上方不生成障碍物（在 `GenerateChunkAtEnd` 中控制）

### 2. 障碍物配置 (`TilemapMapConfig.cs`)

```csharp
[Header("障碍物 Tile 池")]
[Tooltip("障碍物 Tile 池，至少包含一个障碍物 Tile")]
public TileBase[] obstacleTiles;

[Header("障碍物配置")]
[Tooltip("障碍物生成概率 (0-1)")]
[Range(0f, 1f)]
public float obstacleSpawnChance = 0.3f;

[Tooltip("障碍物之间的最小间隔（瓦片数量）")]
public int minObstacleGap = 3;

[Tooltip("障碍物之间的最大间隔（瓦片数量）")]
public int maxObstacleGap = 8;

[Tooltip("障碍物在 Y 轴上的生成层（通常为 groundHeight）")]
public int obstacleLayerY = 5;
```

### 3. MapConfig.asset 配置

```yaml
obstacleTiles:
  - {fileID: 11400000, guid: cc23b4c4f5f2c7849ac5161102d10fe7, type: 2}
  - {fileID: 11400000, guid: 5c1aeb9d3f21f434588b0d1e6c6e3bd9, type: 2}
  - {fileID: 11400000, guid: 31e28ed86a5cd6e4c9214f745703f855, type: 2}
obstacleSpawnChance: 0.3
minObstacleGap: 3
maxObstacleGap: 8
```

---

## 测试结果

### EditMode 测试

**文件:** `Assets/Tests/EditMode/Map/ObstacleGenerationTests.cs`

测试项:
- [x] 障碍物生成在正确高度
- [x] 障碍物间隔符合配置
- [x] 空隙上方不生成障碍物
- [x] 高度差 > 0 时不生成障碍物

**测试覆盖:**
- `ShouldSpawnObstacle` - 概率控制、间隔控制、高度差检测
- `SpawnObstacle` - 生成位置、状态更新
- 空隙互斥检测

---

## 验收标准

- [x] 障碍物可以按概率生成
- [x] 障碍物生成位置正确（草坪层上方）
- [x] 障碍物之间保持合理间隔
- [x] 空隙上方不生成障碍物
- [x] 通过 EditMode 测试验证

---

## 备注

障碍物生成系统已完成实现和测试。3-2 概率控制在之前已完成，本次完成 3-1 核心生成逻辑和测试。
